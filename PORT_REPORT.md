# Port report — `predicates-kotlin`

> Generated by `scaffold/generate_repo_reports.py` from `workspace_dep_graph.json` ⨯ `port_priority.json`. Do not hand-edit. Re-run after the workspace dep graph is rebuilt.

## Overview

- **1** member crate(s): `predicates`
- **Codex-kotlin distance**: **hop 1** from codex-kotlin via cross-repo Rust deps
- **Inbound**: 15 `use` sites from 2 other repos
- **Outbound**: 0 `use` sites to 0 other repos
- **Unresolved external**: 4 published Cargo crates with no `*-kotlin/` port

## Member crates

| package | module | inbound `use` sites | outbound external repos | Jira | rank | status |
|---|---|---:|---:|---|---:|---|
| `predicates` | `predicates` | 15 | 0 | COD-99 | 237 | To Do |

## Inbound — what to port first

Every symbol this repo's crates need to expose, ranked by total workspace demand. **Codex-kotlin sites are listed first per symbol** because codex is the priority consumer driving the port queue. Anything not listed here is not currently `use`'d by any kotlinmania repo and can be deferred.

### Inbound caller summary (15 edges, 4 caller crates)

| caller repo | caller crate | use sites | codex consumer? |
|---|---|---:|---|
| `codex-kotlin` | `codex-cli` | 10 | ✅ |
| `assert-cmd-kotlin` | `assert_cmd` | 2 |  |
| `codex-kotlin` | `codex-exec` | 2 | ✅ |
| `codex-kotlin` | `codex-core` | 1 | ✅ |

### Symbol-level demand (4 distinct symbols)

Sorted by codex demand, then total demand. Symbols with codex-kotlin sites are the priority queue. Each symbol entry below shows up to 12 call sites (codex first).

| symbol | total | codex | other | distinct callers |
|---|---:|---:|---:|---:|
| `predicates::str::contains` | 11 | 11 | 0 | 2 |
| `predicates::prelude::*` | 2 | 1 | 1 | 2 |
| `predicates::prelude::PredicateBooleanExt` | 1 | 1 | 0 | 1 |
| `predicates::str::PredicateStrExt` | 1 | 0 | 1 | 1 |

#### Per-symbol detail (top 4 symbols)

##### `predicates::str::contains` (11 sites, 11 codex, 0 other, 2 caller crates)

Codex call sites:

- `codex-kotlin/codex-cli` `tests/debug_clear_memories.rs:6` — `use predicates::str::contains`
- `codex-kotlin/codex-cli` `tests/features.rs:4` — `use predicates::str::contains`
- `codex-kotlin/codex-cli` `tests/login.rs:4` — `use predicates::str::contains`
- `codex-kotlin/codex-cli` `tests/marketplace_add.rs:4` — `use predicates::str::contains`
- `codex-kotlin/codex-cli` `tests/marketplace_remove.rs:5` — `use predicates::str::contains`
- `codex-kotlin/codex-cli` `tests/marketplace_upgrade.rs:2` — `use predicates::str::contains`
- `codex-kotlin/codex-cli` `tests/mcp_add_remove.rs:6` — `use predicates::str::contains`
- `codex-kotlin/codex-cli` `tests/mcp_list.rs:8` — `use predicates::str::contains`
- `codex-kotlin/codex-cli` `tests/update.rs:2` — `use predicates::str::contains`
- `codex-kotlin/codex-exec` `tests/suite/mcp_required_exit.rs:6` — `use predicates::str::contains`
- `codex-kotlin/codex-exec` `tests/suite/prompt_stdin.rs:6` — `use predicates::str::contains`

##### `predicates::prelude::*` (2 sites, 1 codex, 1 other, 2 caller crates)

Codex call sites:

- `codex-kotlin/codex-core` `tests/suite/live_cli.rs:8` — `use predicates::prelude::*`

Other workspace call sites:

- `assert-cmd-kotlin/assert_cmd` `src/assert.rs:1121` — `use predicates::prelude::*`

##### `predicates::prelude::PredicateBooleanExt` (1 sites, 1 codex, 0 other, 1 caller crates)

Codex call sites:

- `codex-kotlin/codex-cli` `tests/mcp_list.rs:7` — `use predicates::prelude::PredicateBooleanExt`

##### `predicates::str::PredicateStrExt` (1 sites, 0 codex, 1 other, 1 caller crates)

Other workspace call sites:

- `assert-cmd-kotlin/assert_cmd` `src/assert.rs:11` — `use predicates::str::PredicateStrExt`

## Outbound — what this repo depends on

Other kotlinmania repos this repo's Rust source `use`s. Symbol-level detail per target lives in **that target repo's PORT_REPORT.md inbound section** (no duplication). Sorted by Jira rank, then by `use`-site count.

_No outbound edges to other workspace repos._

## Unresolved external dependencies

Published Cargo crates this repo's Rust source `use`s but for which no `<crate>-kotlin/` repo exists. Each is either a missing port to file in Jira, or an intentional external dep (system library / FFI / build-only). If a port lives at a non-conventional repo name, add it to `PACKAGE_REPO_OVERRIDES` in `scaffold/build_workspace_dep_graph.py`.

| package | edges | distinct symbols | importing members in this repo |
|---|---:|---:|---|
| `predicates-tree` | 2 | 1 | `predicates` |
| `float-cmp` | 2 | 2 | `predicates` |
| `predicates-core` | 1 | 1 | `predicates` |
| `normalize-line-endings` | 1 | 1 | `predicates` |
