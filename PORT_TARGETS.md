# Port targets — `predicates-kotlin` (driven by codex-rs demand)

> Generated by `scaffold/generate_port_targets.py` from `codex_external_usage.json`. Do not hand-edit. Re-run after rebuilding the usage data.

This is the **minimum subset** of `predicates` that codex-rs uses, and therefore the minimum subset that `predicates-kotlin` needs to port in order for codex-kotlin to compile and run. Symbols are sorted by codex demand (most-used first). Anything not listed here is not currently required by codex — defer it.

**Jira Initiative**: [COD-99] (rank `237`, tier `direct`, status `To Do`)

**Cargo package name**: `predicates`
**Rust module name (in `use` statements)**: `predicates`

## Summary

- **3** distinct symbols imported by codex-rs
- **13** total `use` statements
- **12** codex source files affected
- **3** codex member crates transitively depend on this

## Granularity caveat

This list is **import-level**, not call-site-level. A symbol appearing here means codex `use`'d it; the specific functions, methods, fields, or trait impls codex calls on the imported name are not enumerated. For an initial port, implement the symbol's full API as defined in upstream Rust. For a tighter minimum, drill into each codex source file listed below and grep for the imported name to find concrete invocations — the graph tells you exactly which files to grep.

## Symbols by codex demand (port these in order)

### `predicates::str::contains`

- **11 `use` site(s)** across **2 codex member crate(s)**: `codex-cli`, `codex-exec`

Codex source locations (relative to `tmp/codex/codex-rs/`):

- `cli/tests/debug_clear_memories.rs:6` (`codex-cli`) — `use predicates::str::contains`
- `cli/tests/features.rs:4` (`codex-cli`) — `use predicates::str::contains`
- `cli/tests/login.rs:4` (`codex-cli`) — `use predicates::str::contains`
- `cli/tests/marketplace_add.rs:4` (`codex-cli`) — `use predicates::str::contains`
- `cli/tests/marketplace_remove.rs:5` (`codex-cli`) — `use predicates::str::contains`
- `cli/tests/marketplace_upgrade.rs:2` (`codex-cli`) — `use predicates::str::contains`
- `cli/tests/mcp_add_remove.rs:6` (`codex-cli`) — `use predicates::str::contains`
- `cli/tests/mcp_list.rs:8` (`codex-cli`) — `use predicates::str::contains`
- `cli/tests/update.rs:2` (`codex-cli`) — `use predicates::str::contains`
- `exec/tests/suite/mcp_required_exit.rs:6` (`codex-exec`) — `use predicates::str::contains`
- `exec/tests/suite/prompt_stdin.rs:6` (`codex-exec`) — `use predicates::str::contains`

### `predicates::prelude::*`

- **1 `use` site(s)** across **1 codex member crate(s)**: `codex-core`

Codex source locations (relative to `tmp/codex/codex-rs/`):

- `core/tests/suite/live_cli.rs:8` (`codex-core`) — `use predicates::prelude::*`

### `predicates::prelude::PredicateBooleanExt`

- **1 `use` site(s)** across **1 codex member crate(s)**: `codex-cli`

Codex source locations (relative to `tmp/codex/codex-rs/`):

- `cli/tests/mcp_list.rs:7` (`codex-cli`) — `use predicates::prelude::PredicateBooleanExt`
