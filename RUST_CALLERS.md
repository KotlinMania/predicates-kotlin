# Rust callers — `predicates-kotlin`

> Generated by `scaffold/generate_caller_reports.py` from `workspace_dep_graph.json` (Cargo-aware dep graph). Do not hand-edit. Re-run after the graph is rebuilt.

## What this file is

For every Rust crate this repo owns (under `tmp/`), this file lists every **cross-repo** workspace caller: every `use` statement in another `*-kotlin` repo's Rust source that imports a symbol from one of this repo's crates. Symbols are grouped per-crate; codex-kotlin sites are surfaced first per symbol (codex is the priority consumer driving the port queue).

**Granularity:** import-level (each row is one `use` line). Call-site detail — where the imported name is actually invoked, implemented, pattern-matched, or used as a generic bound inside the caller body — requires a per-symbol grep across the listed caller files. The graph tells you exactly which files to grep.

**Cross-repo only.** Imports between crates inside this same repo are omitted — those are intra-repo refactoring concerns, not the cross-project porting concern this file exists to surface.

**Cargo-resolved.** Counts here honor `package = "..."` renames, `workspace = true` inheritance, and `[target.<cfg>.dependencies]`. Stdlib (`std`, `core`, `alloc`) and intra-crate sub-module references are excluded.

## Crates in this repo

| package | module | inbound use sites | distinct caller crates | distinct caller repos | codex sites |
|---|---|---:|---:|---:|---:|
| `predicates` | `predicates` | 15 | 4 | 2 | 13 |

## `predicates`

### Inbound symbols (4 distinct, 15 total `use` sites)

Sorted by codex demand, then total demand. The first symbols are the priority queue for porting `predicates`.

#### `predicates::str::contains` — 11 site(s) (11 codex, 0 other, 2 caller crates)

- ✅ `codex-kotlin/codex-cli` `tests/debug_clear_memories.rs:6` — `use predicates::str::contains`
- ✅ `codex-kotlin/codex-cli` `tests/features.rs:4` — `use predicates::str::contains`
- ✅ `codex-kotlin/codex-cli` `tests/login.rs:4` — `use predicates::str::contains`
- ✅ `codex-kotlin/codex-cli` `tests/marketplace_add.rs:4` — `use predicates::str::contains`
- ✅ `codex-kotlin/codex-cli` `tests/marketplace_remove.rs:5` — `use predicates::str::contains`
- ✅ `codex-kotlin/codex-cli` `tests/marketplace_upgrade.rs:2` — `use predicates::str::contains`
- ✅ `codex-kotlin/codex-cli` `tests/mcp_add_remove.rs:6` — `use predicates::str::contains`
- ✅ `codex-kotlin/codex-cli` `tests/mcp_list.rs:8` — `use predicates::str::contains`
- ✅ `codex-kotlin/codex-cli` `tests/update.rs:2` — `use predicates::str::contains`
- ✅ `codex-kotlin/codex-exec` `tests/suite/mcp_required_exit.rs:6` — `use predicates::str::contains`
- ✅ `codex-kotlin/codex-exec` `tests/suite/prompt_stdin.rs:6` — `use predicates::str::contains`

#### `predicates::prelude::*` — 2 site(s) (1 codex, 1 other, 2 caller crates)

- ✅ `codex-kotlin/codex-core` `tests/suite/live_cli.rs:8` — `use predicates::prelude::*`
- `assert-cmd-kotlin/assert_cmd` `src/assert.rs:1121` — `use predicates::prelude::*`

#### `predicates::prelude::PredicateBooleanExt` — 1 site(s) (1 codex, 0 other, 1 caller crates)

- ✅ `codex-kotlin/codex-cli` `tests/mcp_list.rs:7` — `use predicates::prelude::PredicateBooleanExt`

#### `predicates::str::PredicateStrExt` — 1 site(s) (0 codex, 1 other, 1 caller crates)

- `assert-cmd-kotlin/assert_cmd` `src/assert.rs:11` — `use predicates::str::PredicateStrExt`
